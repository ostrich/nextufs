Seed assumptions:
- clean seed contains `/dir`

Corruption:
- clear the root-directory entry for `dir` without clearing the inode

Primary expectation:
- pass 3 treats `/dir` as disconnected and later repair may reconnect it
